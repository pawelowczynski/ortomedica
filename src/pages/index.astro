---
import Layout from '../layouts/layout.astro';
import Header from '../components/header.astro';
import Footer from '../components/footer.astro';
import Cennik from '../components/Cennik.astro';
import Hero from '../components/Hero.astro';
import OfferGrid from '../components/OfferGrid.astro';
import ProcessSteps from '../components/ProcessSteps.astro';
import Team from '../components/Team.astro';
import ClinicGallery from '../components/ClinicGallery.astro';
import LocalSEO from '../components/LocalSEO.astro';
import FAQSection from '../components/FAQSection.astro';
import ContactSection from '../components/ContactSection.astro';
import { canonicalUrl, homepageGraphJsonLd } from '../data/schema';
import { homePageFaqs } from '../data/faq';

const faqSchema = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: homePageFaqs.map((item) => ({
    '@type': 'Question',
    name: item.q,
    acceptedAnswer: {
      '@type': 'Answer',
      text: item.a,
    },
  })),
};
---

<Layout 
    title="ORTHOMEDICA Lubin | Ortodoncja, stomatologia dziecięca, RTG w gabinecie"
    description="ORTHOMEDICA w Lubinie: ortodoncja i stomatologia dziecięca u dr n. med. Kornelia Rumin. RTG i skan 3D (Shining) na miejscu, od konsultacji po spójny plan leczenia. Umów wizytę w dogodnym terminie."
    canonical={canonicalUrl('/')}
>
    <script slot="head" type="application/ld+json" set:html={JSON.stringify(faqSchema)} />
    <script slot="head" type="application/ld+json" set:html={JSON.stringify(homepageGraphJsonLd())} />

    <Header />
    
    <main id="main-content">
        <Hero />
        <OfferGrid />
        <ProcessSteps />
        <Team />
        <ClinicGallery />

        <Cennik />

        <FAQSection />

        <ContactSection />
        <LocalSEO />
    </main>

    <Footer />
</Layout>
